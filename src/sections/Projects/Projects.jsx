import React from 'react'

const Projects = ({sectionRefs}) => {
  return (
    <section id="projects" ref={sectionRefs.projects} className="min-h-screen bg-gray-500">
      
    </section>
  )
}

export default Projects
