import React from 'react'

const About = ({sectionRefs}) => {
  return (
    <section id="about" ref={sectionRefs.about} className="min-h-screen bg-gray-400">
      
    </section>
  )
}

export default About
